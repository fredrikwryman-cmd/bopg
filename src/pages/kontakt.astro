---
import Layout from '../layouts/Layout.astro';
import { company } from '../data/company.js';
import Navbar from '../components/layout/Navbar.jsx';
import Footer from '../components/layout/Footer.jsx';
import PageHeader from '../components/pages/PageHeader.jsx';
import ContactSection from '../components/contact/ContactSection.jsx';
---

<Layout
  title={`Kontakt | ${company.legalName}`}
  description={`Kontakta ${company.name} i Åkersberga. Berätta om ditt projekt så återkommer vi med en offert så snart vi kan.`}
>
  <Navbar client:idle />
  <main>
    <PageHeader
      client:load
      eyebrow="Kontakt"
      title="Låt oss prata om ditt projekt"
      subtitle="Berätta kort vad du planerar, så tar vi nästa steg tillsammans."
      crumbs={[{ label: 'Kontakt' }]}
    />
    {/* showHeading={false}: PageHeader ovanför visar redan eyebrow "Kontakt",
        rubriken och samma ingress. Utan flaggan renderas hela blocket två
        gånger i rad — först mörkt, sedan ljust. Ändras rubriken här måste
        ContactSection.jsx följa med, annars säger sidhuvudet och sektionen
        olika saker. */}
    <ContactSection client:visible showHeading={false} subject="Ny förfrågan från byggoprojektgruppen.se (Kontakt)" />
  </main>
  <Footer />
</Layout>
